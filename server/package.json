{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "start": "node -r ts-node/register .",
    "start-dapr": "dapr run --app-protocol grpc --app-id math-test-server --app-port 9085 --config ../dapr.yaml ts-node index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.3.0",
    "dapr-client": "^0.12.0",
    "protogen": "file:../gen/nodets"
  }
}
